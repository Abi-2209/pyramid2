# pyramid2
This code loads an image using OpenCV, creates a Gaussian pyramid (multiple smaller versions), and displays them with Matplotlib. It then applies Canny edge detection at each pyramid level to show how edges change with resolution.
